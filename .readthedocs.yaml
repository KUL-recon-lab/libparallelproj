version: 2

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "miniconda-latest"
  commands:
    - mamba run -n readthedocs make -C docs doxygen

conda:
  environment: docs/environment.yaml

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: docs/conf.py
