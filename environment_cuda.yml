name: libparallelproj_cuda

channels:
  - conda-forge

dependencies:
  - cxx-compiler
  - cuda-compiler
  - cmake
  - ninja
  - nanobind
  - python >=3.12
  - numpy
  - array-api-compat
  - array-api-strict
  - pytest
  - cupy
  - pytorch=*=cuda* # optional to test with pytorch
